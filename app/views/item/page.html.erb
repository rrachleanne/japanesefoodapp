<h1>Menu</h1>
<div class="row">
    <% @menus.each do |menu| %>
        <div class="card" >
        <%= image_tag menu.picture, class:"card-img-top" %>
        <div class="card-body">
          <h5 class="card-title"><%= menu.nmae %></h5>
          <p class="card-text"><%= menu.description %></p>
          <p class="card-text">$<%= menu.price %></p>
          <a href="#" class="btn btn-success">Add to Cart</a>
        </div>
      </div>
        <% end %>
    </div>